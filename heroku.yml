setup:
  addons:
    - plan: heroku-postgresql
      as: EVENT_STORE
    - plan: heroku-postgresql
      as: READ_STORE
build:
  docker:
    web: Dockerfile
  config:
    MIX_ENV: prod
run:
  web: elixir --sname devton -S mix phx.server
